# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Sidebar consolidation strategy | Agrupar telas por abas dentro de páginas existentes (Tabs do shadcn/ui) | Mantém URLs simples, não quebra bookmarks, usa componente já disponível no projeto. Alternativas: nested routes (mais complexo) ou dropdown menus (esconde funcionalidades). | Yes | collaborative |
| D002 |  | architecture | Dashboard do dia como página principal | /dashboard mostra visão do dia por padrão com toggle para visão mensal | Freelancer precisa da visão diária como porta de entrada; visão mensal continua acessível mas não é o default. Alternativas: rota separada /hoje (fragmenta) ou widget no topo (pouco destaque). | Yes | collaborative |
| D003 |  | architecture | Error handling strategy | Toast genérico + console log para erros de query; estados vazios amigáveis; TypeScript strict mode | App single-user — toast + console é suficiente sem over-engineering. Error boundaries e Sentry são overkill nesse estágio. | Yes | collaborative |
| D004 |  | architecture | Remover journaling/mood feature | Remover daily_journal e todas as referências; Diário na sidebar vira Tracking (Logs) | Tracking de horas é essencial para freelancer; journaling com mood é ruído que compete por atenção na navegação. | Yes | human |
| D005 |  | architecture | How to handle old routes after tab consolidation (/disponibilidade, /folgas, /pipeline) | Keep standalone route page.tsx files intact for backward compatibility — old URLs continue to work but are no longer in the sidebar | Preserves bookmarks and any external links. Zero cost to keep them since the server components are trivial. Users who navigate via old URLs see the full-page version; sidebar users see the tab version. No redirects needed. | yes — can add redirects later if analytics show old routes have zero traffic | agent |
| D006 |  | architecture | Dashboard server/client split architecture for daily+monthly tabs | Keep page.tsx as server component for all data fetching (daily + monthly), pass serialized props to a new dashboard-client.tsx client component that manages Tabs state. Extract current monthly rendering into monthly-view.tsx, new daily widgets into daily-view.tsx. Revenue-at-risk is a standalone client component. | Server component must remain for Supabase auth and server-side queries. Tabs require client state. This split keeps the Next.js server/client boundary clean — data fetching on the server, interactivity on the client. Extracting views into separate files keeps components under 200 lines each. | Yes | agent |
| D007 |  | architecture | Revenue-at-risk widget calculation approach for S03 | Client-side staleness computation: fetch all active jobs + daily_logs from last 14 days in the server component, pass to revenue-at-risk widget which computes which jobs have no recent log. Display job name, client, rate, and days since last log — no speculative dollar amounts. | Avoids Supabase RPC/view (overkill for MVP), keeps queries simple and parallelizable. Showing factual data (rate, days idle) is more actionable than speculative revenue loss calculations. Active jobs + 14-day logs are small datasets for a freelancer. | Yes | agent |
| D008 |  | architecture | Pagination strategy for list pages | Hybrid SSR + client load-more: server component fetches first 25 items with .range(0,24) and { count: 'exact' }, client component uses usePaginatedList hook for subsequent pages via browser Supabase client | Preserves SSR first-paint performance, minimal disruption to existing server/client split pattern, avoids converting pages to fully client-rendered. Offset-based pagination with .range() is sufficient for expected data volumes (hundreds of rows, not millions). CSV export fetches all data independently to avoid the loaded-items limitation. | Yes | agent |
| D009 |  | architecture | How portal API routes authenticate and access data | Create a Supabase admin client using SUPABASE_SERVICE_ROLE_KEY for portal API routes. Token validation happens first, then service-role client fetches scoped data. No broad public-read RLS policies on financial tables. | The anon key cannot bypass RLS on invoices/jobs/clients tables (auth.uid() = user_id). Service role is safer than adding public-read policies to financial tables — the route validates the portal token first and only fetches data belonging to that token's client_id/user_id pair. | no — this is a security boundary decision | agent |
| D010 |  | architecture | How client payment confirmation is recorded | Add client_confirmed_at timestamptz column to invoices table via new migration. Client confirms they paid by setting this timestamp. This is separate from the freelancer's PaymentDialog flow which inserts into invoice_payments. | Simplest approach — avoids RLS complexity on invoice_payments, keeps freelancer payment tracking separate from client's claim. A new column on invoices is lightweight and queryable. The freelancer still manually verifies and records actual payment. | yes — could later add a notification when client confirms | agent |
